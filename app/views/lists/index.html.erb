<h1>Lists</h1>
<% @lists.each do |list| %>
  <%= link_to list_path(list) do%>
    <li><%= list.name %></li>
  <% end %>
<% end %>
<%= link_to 'Add a list', new_list_path, class: 'btn-light text-decoration-none'%>
